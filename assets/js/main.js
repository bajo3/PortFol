window.addEventListener("load", function () {
    // Oculta el loader una vez que la página se haya cargado completamente.
    const loader = document.getElementById("loader");
    loader.style.display = "none";
});